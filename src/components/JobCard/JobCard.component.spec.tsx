import React from "react";
import { shallow } from "enzyme";
import { JobCard } from "./JobCard.component";

describe("JobCard", () =>{
    it("render tile layout for job card", () => {
        // Job card have date, so snapshot will updated every time 
        // const jobCard = shallow();
    });
});